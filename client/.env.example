# Generated by bootstrapping-project.sh - copy to tweak; .env is gitignored.
NODE_ENV=development

# Vite (public vars must be prefixed VITE_)
VITE_APP_TITLE=NihongoCoach
# Dev: leave empty to use Vite proxy (/api -> :4000, refresh cookie reliable)
# Docker/VPS (nihongo.com.vn + edge nginx same-origin): leave empty
# Only set if SPA and API are on different origins, e.g.:
#   VITE_API_BASE_URL=https://api.nihongo.com.vn
VITE_API_BASE_URL=
